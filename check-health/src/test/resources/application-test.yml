# Configurações específicas para testes
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    defer-datasource-initialization: false
    properties:
      hibernate:
        format_sql: true

  # Configuração simplificada do Kafka para testes
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  docker:
    compose:
      enabled: false
  
  # Desabilita Spring Cloud Config para testes
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false

# Desabilita Eureka Client para testes
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

# Desabilitar alguns recursos para acelerar os testes
logging:
  level:
    com.fiap.check.health: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG

# Configurações de teste específicas
test:
  mock:
    kafka: true